/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add image domains to display Gig images you upload
    domains: [
      "media.istockphoto.com",
      "t3.ftcdn.net",
      "adorable-donkey-58.convex.cloud",
      "upload.wikimedia.org",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
